import { View, Text } from "react-native";
import styles from "../addButton/style.addButton";

const AddButton = () => {
  return <View style={styles.container}></View>;
};

export default AddButton;
