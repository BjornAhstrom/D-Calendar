import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    borderColor: "pink",
    borderWidth: 2,
    borderRadius: 10,
    width: "90%",
    height: "90%",
    alignSelf: "center",
  },
});

export default styles;
