import { View, Text } from "react-native";
import CustomCalendar from "../components/calendar/calendar";

const Home = () => {
  return (
    <View>
      <Text>Home</Text>
      <CustomCalendar />
    </View>
  );
};

export default Home;
