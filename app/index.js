import { View, Text } from "react-native";
import MainCalendar from "../components/mainCalendar/mainCalendar";

const Home = () => {
  return (
    <View>
      <MainCalendar />
    </View>
  );
};

export default Home;
