import { View, Text } from "react-native";
import CustomCalendar from "../components/calendar/calendar";

const Home = () => {
  return (
    <View>
      <CustomCalendar />
    </View>
  );
};

export default Home;
